// TEMPORARILY DISABLED - Prisma client not generated yet
// This file will be re-enabled once Prisma dependencies are properly installed

/*
import { Injectable, OnModuleInit } from '@nestjs/common';
import { PrismaClient } from '@prisma/client';

@Injectable()
export class PrismaService extends PrismaClient implements OnModuleInit {
  async onModuleInit() {
    await this.$connect();
  }
}
*/ 